[project]
name = "mt5-bridge"
version = "1.0.0"
description = "MetaTrader 5 Python bridge for trading dashboard"
requires-python = ">=3.8"
dependencies = [
    "MetaTrader5>=5.0.0",
]

[tool.uv]
python-version = "3.8"
